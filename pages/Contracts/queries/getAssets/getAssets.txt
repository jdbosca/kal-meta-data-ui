SELECT ASSET_ID, ASSET_TYPE, MINE_REF
FROM VIEW_DB.DATA_SCHEMA.ASSETS_V
WHERE 
    ASSET_TYPE IN ({{MultiSelect1.selectedOptionValues.map(value => `'${value}'`).join(', ')}})
    AND 
    MINE_REF = '{{mine_select.selectedOptionValue}}';
/